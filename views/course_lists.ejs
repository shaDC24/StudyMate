<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Course Lists</title>
    <!-- Add Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        /* Custom styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #dce3eb;
            padding: 20px;
        }

        h1 {
            color: #343a40;
            margin-bottom: 20px;
        }

        h2 {
            color: #343a40;
            margin-bottom: 20px;
        }

        .accordion-button {
            background-color: #e6e1db;
            color: #343a40;
            border-radius: 0;
            border: none;
        }

        .accordion-button:focus {
            box-shadow: none;
        }

        .accordion-button:not(.collapsed) {
            background-color: #6c757d;
            color: #fff;
        }

        .accordion-body {
            background-color: #fff;
            border: 1px solid #dee2e6;
            border-top: none;
            border-radius: 0 0 .25rem .25rem;
            padding: 10px;
        }

        .teacher {
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Course Lists</h1>
        <h2>Hi <%= courses[0].first_name %>!!!</h2>
        <% for (let i=0; i < courses.length; i++) { %>
            <div class="accordion accordion-flush" id="accordionFlushExample<%= i %>">
                <div class="accordion-item">
                    <h2 class="accordion-header" id="flush-heading<%= i %>">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse"
                            data-bs-target="#flush-collapse<%= i %>" aria-expanded="false"
                            aria-controls="flush-collapse<%= i %>">
                            <%= courses[i].course_title %>
                        </button>
                    </h2>
                    <div id="flush-collapse<%= i %>" class="accordion-collapse collapse"
                        aria-labelledby="flush-heading<%= i %>" data-bs-parent="#accordionFlushExample<%= i %>">
                        <div class="accordion-body">
                            <%= courses[i].course_description %>
                        </div>
                        <div class="teacher">
                            <%= courses[i].teacher_name %>
                        </div>
                        <div class="mt-3">
                            <a href="/show/lecture?course_id=<%= courses[i].id %>">
                                <button class="btn btn-secondary">Show Lectures</button>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>
    </div>

    <!-- Bootstrap JavaScript and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"></script>
</body>

</html>