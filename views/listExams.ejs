<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exam-wise Leaderboard</title>
    <!-- Add your CSS styles here for bubbles -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
            margin: 0;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .exams-container {
            text-align: center;
        }

        .bubble {
            display: inline-block;
            margin: 10px;
            padding: 15px;
            border-radius: 50%;
            background-color: #3498db;
            color: #fff;
            font-size: 18px;
            cursor: pointer;
        }

        .navbar {
            background-color: #007bff;
            overflow: hidden;
            margin-bottom: 20px;
        }

        .navbar button {
            background-color: inherit;
            border: none;
            color: white;
            padding: 14px 20px;
            cursor: pointer;
            float: left;
            transition: background-color 0.3s;
        }

        .navbar button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <div class="navbar">
        <button onclick="window.location.href='/student/dashboard'">Home</button>
        <button onclick="window.location.href='/exam_section/result'">Result</button>
        <button onclick="window.location.href='/exam_section/results'">Progress</button>
        <button onclick="window.location.href='/student/exam-section'">Exam Section</button>
        <button onclick="window.location.href='/leaderboard'">Leaderboard</button>
    </div>

    <h1>Exam-wise Leaderboard</h1>
    <div class="exams-container">
        <% exams.forEach(exam => { %>
            <a href="/leaderboard/<%= exam.id %>">
                <div class="bubble"><%= exam.exam_topic %></div>
            </a>
        <% }); %>
    </div>
</body>
</html>
