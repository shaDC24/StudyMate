<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exam-wise Leaderboard</title>
    <!-- Add your CSS styles here for bubbles -->
    <style>
        .bubble {
            display: inline-block;
            margin: 10px;
            padding: 15px;
            border-radius: 50%;
            background-color: #3498db;
            color: #fff;
            font-size: 18px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>Exam-wise Leaderboard</h1>
    <div class="exams-container">
        <% exams.forEach(exam => { %>
            <a href="/leaderboard/<%= exam.id %>">
                <div class="bubble"><%= exam.exam_topic %></div>
            </a>
        <% }); %>
    </div>
</body>
</html>
