<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Lectures</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #333;
        }

        ul {
            list-style: none;
            padding: 0;
        }

        li {
            background-color: #f9f9f9;
            margin-bottom: 10px;
            padding: 15px;
            border-radius: 8px;
            cursor: pointer;
        }

        li:hover {
            background-color: #eaeaea;
        }

        .video-container {
            display: none;
            margin-top: 10px;
        }

        iframe,
        video {
            width: 100%;
            height: 315px;
            border-radius: 8px;
        }

        button {
            display: block;
            margin: 20px auto;
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Show Lectures for <%= course.course_title %></h1>

        <ul>
            <% for (let i = 0; i < lectures.length; i++) { %>
                <li onclick="showDetails(<%=i%>)">
                    <strong>Lecture Name:</strong> <%= lectures[i].lecture_name %><br>
                    <strong>Description:</strong> <%= lectures[i].description %><br>
                    <%= lectures[i].videolink %><br>
                    <div class="video-container" id="video<%= i %>">
                        <% if (lectures[i].videolink.includes('youtube.com') || lectures[i].videolink.includes('youtu.be')) { %>
                            <iframe width="560" height="315" src="https://www.youtube.com/embed/VIDEO_ID?privacy=1" frameborder="0" allowfullscreen></iframe>
                        <% } else { %>
                            <video controls>
                                <source src="<%= lectures[i].videolink %>" type="video/mp4">
                                Your browser does not support the video tag.
                            </video>
                        <% } %>
                    </div>
                </li>
            <% } %>
        </ul>

        <a href="/teacher/lecture">
            <button>Back to Courses</button>
        </a>
    </div>

    <script>
        function showDetails(index) {
            var videoContainer = document.getElementById('video' + index);
            if (videoContainer.style.display === 'none') {
                videoContainer.style.display = 'block';
            } else {
                videoContainer.style.display = 'none';
            }
        }
    </script>
</body>

</html>


