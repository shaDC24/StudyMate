<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Lectures</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        li {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-bottom: 20px;
        }

        li:hover {
            box-shadow: 0 6px 8px rgba(0, 0, 0, 0.2);
        }

        strong {
            color: #333;
        }

        video {
            width: 100%;
            border-radius: 5px;
            margin-top: 10px;
        }

        a {
            text-decoration: none;
            display: inline-block;
            margin-top: 20px;
        }

        button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            background-color: #007bff;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <h1>Show Lectures for <%= course.course_title %></h1>

    <ul>
        <% for (let i = 0; i < lectures.length; i++) { %>
            <li>
                <strong>Lecture Name:</strong> <%= lectures[i].lecture_name %><br>
                <strong>Description:</strong> <%= lectures[i].description %><br>
                
                <video width="320" height="240" controls>
                    <source src="/uploads/video_<%= lectures[i].lecture_id %>_<%= lectures[i].course_id %>_<%= lectures[i].teacher_id %>.mp4" type="video/mp4">
                    Your browser does not support the video tag.
                </video>
            </li>
        <% } %>
    </ul>

    <a href="/teacher/lecture">
        <button>Back to Courses</button>
    </a>

</body>

</html>
